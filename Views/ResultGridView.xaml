<UserControl x:Class="UniversalSearchCriteria.Views.ResultGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:UniversalSearchCriteria.ViewModels"
             xmlns:extensions="clr-namespace:UniversalSearchCriteria.Extensions"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" ItemsSource="{Binding FilteredItems}" Margin="4,5,229,0" MaxHeight="260" VerticalScrollBarVisibility="Auto">
            <DataGrid.Resources>
                <Style TargetType="DataGrid">
                    <Setter Property="extensions:DataGridExtensions.AutoGenerateDateTimeColumns" Value="True" />
                </Style>
            </DataGrid.Resources>
        </DataGrid>
    </Grid>
</UserControl>
